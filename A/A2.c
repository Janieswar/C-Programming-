// add two numbers

#include<stdio.h>
int main() {         // Start of Program
    int a = 10;      // declearing int variable for a 
    int b = 20;      // declearing int variable for b
    int sum = a+b;   // Storing the a and b inside sum variable
    printf("Sum = %d", sum);  // Printing the sum (Used %d)
    return 0;
}